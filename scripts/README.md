# Scripts

Optional helper scripts can live here.

The current v1 intentionally keeps the workflow manual and Markdown-first. Add scripts only when repeated dream-sequence runs reveal a stable need, such as link checking, source hashing, or report generation.
